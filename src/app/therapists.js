"use client"
import { motion } from "framer-motion";

export default function Services() {
    return (
        <section className="relative z-10 border-white flex flex-col items-center justify-center min-h-screen bg-black/50 border-b">

        </section>
    );
}